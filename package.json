{
  "name": "optimus-monorepo",
  "private": true,
  "version": "1.0.0",
  "description": "Optimus TypeScript Monorepo (pnpm workspaces + Turborepo)",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=9.0.0"
  },
  "packageManager": "pnpm@10.30.3",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "clean": "turbo run clean && rimraf node_modules"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "turbo": "^2.4.4",
    "typescript": "^5.8.3"
  }
}
